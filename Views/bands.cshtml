<h1>Band Tracker</h1>
<br>
<h2>Band List</h2>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var band in Model)
    {
      <li><a href='/bands/@band.GetId()'>@band.GetName()</a></li>
    }
  </ul>
}
<h3<a href="/bands/new">Create Band for you List</a></h3>
<br>
<h4><a href="/">Home</a></h4>
